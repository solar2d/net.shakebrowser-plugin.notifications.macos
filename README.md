Sources for the plugin `plugin.notifications.macos`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.notifications.macos" = {
            publisherId = "net.shakebrowser",
        },
    },
}
```
